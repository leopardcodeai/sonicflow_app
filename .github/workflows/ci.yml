name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  core-js:
    runs-on: macos-15
    defaults:
      run:
        working-directory: sonicflow_app/core-js
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm test
