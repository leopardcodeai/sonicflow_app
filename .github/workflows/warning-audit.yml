name: Warning Audit

on:
  workflow_dispatch:

jobs:
  warning-audit:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: ./scripts/check_warnings.sh
