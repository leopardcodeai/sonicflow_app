# FlowTones mac-app

This folder is reserved for a dedicated macOS app module.

Current macOS implementation and extension host targets are still located in:

- `sonicflow_app/safari-extension/FlowTones/FlowTones.xcodeproj`

When a standalone macOS app project is split out, keep platform-specific assets and build instructions here.
